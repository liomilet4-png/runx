skill: echo
harness:
  cases:
    - name: agent-boundary
      runner: default
      expect:
        status: sealed
runners:
  default:
    type: agent
    default: true
    agent: fixture
    task: echo
    outputs:
      result: object
