name: echo-basic
lane: deterministic
target:
  kind: tool
  ref: docs.echo
inputs:
  message: hello
expect:
  status: sealed
  output:
    subset:
      schema: docs.demo.echo.v1
      data:
        message: hello
