skill: managed-agent-intake

catalog:
  kind: skill
  audience: public
  visibility: public

runners:
  intake:
    default: true
    type: agent-task
    agent: builder
    task: triage-intake
    outputs:
      summary: object
    inputs:
      issue_title:
        type: string
        required: false
